name: saar
description: "SAAR - Smart Automated Action Replay"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.0.0
  sqflite: ^2.4.2
  path: ^1.9.1
  http: ^1.3.0
  provider: ^6.1.4
  uuid: ^4.5.1
  flutter_dotenv: ^5.2.1
  path_provider: ^2.1.5
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
